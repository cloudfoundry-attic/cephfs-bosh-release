set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Available variables
# $BOSH_COMPILE_TARGET - where this package & spec'd source files are available
# $BOSH_INSTALL_TARGET - where you copy/install files to be included in package

#mkdir -p $BOSH_INSTALL_TARGET/apt

#for DEB in $(ls -1 apt/python-dependencies/*.deb); do
#  echo "Installing $(basename $DEB)"
#  dpkg -x $DEB $BOSH_INSTALL_TARGET/apt
#done

cp -a apt/python-dependencies/libevent-dev_2.0.21-stable-1ubuntu1.14.04.1_amd64.deb $BOSH_INSTALL_TARGET/
cp -a apt/python-dependencies/liblzma-dev_5.1.1alpha+20120614-2ubuntu2_amd64.deb $BOSH_INSTALL_TARGET/
cp -a apt/python-dependencies/libssl-dev_1.0.1f-1ubuntu2.16_amd64.deb $BOSH_INSTALL_TARGET/
cp -a apt/python-dependencies/profile.sh $BOSH_INSTALL_TARGET/
